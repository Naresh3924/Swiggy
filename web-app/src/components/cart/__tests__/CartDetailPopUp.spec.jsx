import { render } from "@testing-library/react";
import CartDetailPopUp from "../CartDetailPopUp";

describe("Test case for CardDetailspopup compoenents", () => {
  test("Should CardDetailspopup render", () => {
    render(<CartDetailPopUp />);
  });
});

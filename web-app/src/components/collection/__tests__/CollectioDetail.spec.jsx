import { render } from "@testing-library/react";
import CollectionDetail from "../CollectionDetail";

describe("Testcase for CollectionDetails", () => {
  test("should render collectiondetails component", () => {
    render(<CollectionDetail />);
  });
});

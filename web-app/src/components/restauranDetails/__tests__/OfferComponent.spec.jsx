import { render } from "@testing-library/react";
import OfferComponent from "../OfferComponent";

describe("TestCase for OfferComponent", () => {
  test("should render OfferComponent component", () => {
    render(<OfferComponent />);
  });
});

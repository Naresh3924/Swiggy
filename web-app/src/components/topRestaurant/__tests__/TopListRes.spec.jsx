import { render } from "@testing-library/react";
import TopListRes from "../TopListRes";

describe("TestCase for TopListRes", () => {
  test("should render TopListRes component", () => {
    render(<TopListRes />);
  });
});

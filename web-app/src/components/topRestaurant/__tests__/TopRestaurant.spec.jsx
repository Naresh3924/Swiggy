import { render } from "@testing-library/react";
import TopRestaurant from "../TopRestaurant";

describe("TestCase for TopRestaurant", () => {
  test("should render TopRestaurant component", () => {
    render(<TopRestaurant />);
  });
});

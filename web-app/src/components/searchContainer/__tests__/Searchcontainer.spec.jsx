import { render } from "@testing-library/react";
import Searchcontainer from "../Searchcontainer";

describe("TestCase for Searchcontainer", () => {
  test("should render Searchcontainer component", () => {
    render(<Searchcontainer />);
  });
});

import { render } from "@testing-library/react";
import RestaurantDetail from "../RestaurantDetail";

describe("TestCase for RestaurantDetail", () => {
  test("should render RestaurantDetail component", () => {
    render(<RestaurantDetail />);
  });
});

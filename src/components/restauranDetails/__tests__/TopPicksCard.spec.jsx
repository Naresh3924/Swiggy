import { render } from "@testing-library/react";
import TopPicksCard from "../TopPicksCard";

describe("TestCase for TopPicksCard", () => {
  test("should render TopPicksCard component", () => {
    render(<TopPicksCard />);
  });
});

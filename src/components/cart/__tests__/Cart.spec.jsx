import { render } from "@testing-library/react";
import Cart from "../Cart";

describe("TestCases for Cart componenent", () => {
  test("should render Cart cmponent", () => {
    render(<Cart />);
  });
});

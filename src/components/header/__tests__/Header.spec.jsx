import { render } from "@testing-library/react";
import Header from "../Header";

describe("TestCases for Header componenent", () => {
  test("should render Header cmponent", () => {
    render(<Header />);
  });
});

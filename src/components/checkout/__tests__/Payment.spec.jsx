import { render } from "@testing-library/react";
import Payment from "../Payment";

describe("Test case for Payment compoenents", () => {
  test("Should Payment render", () => {
    render(<Payment />);
  });
});

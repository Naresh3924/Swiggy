import { render } from "@testing-library/react";
import Account from "../Account";

describe("Test case for Account compoenents", () => {
  test("Should Account render", () => {
    render(<Account />);
  });
});

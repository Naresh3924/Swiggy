import { render } from "@testing-library/react";
import CheckOut from "../CheckOut";

describe("Test case for CheckOut compoenents", () => {
  test("Should CheckOut render", () => {
    render(<CheckOut />);
  });
});

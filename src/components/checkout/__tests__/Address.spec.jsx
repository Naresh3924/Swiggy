import { render } from "@testing-library/react";
import Address from "../Address";

describe("Test case for Address compoenents", () => {
  test("Should Address render", () => {
    render(<Address />);
  });
});

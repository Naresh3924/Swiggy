import { render } from "@testing-library/react";
import Emptycart from "../Emptycart";

describe("Test case for Emptycart compoenents", () => {
  test("Should Emptycart render", () => {
    render(<Emptycart />);
  });
});

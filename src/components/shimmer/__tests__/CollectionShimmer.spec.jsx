import { render } from "@testing-library/react";
import CollectionShimmer from "../collectionShimmer";

describe("TestCase for CollectionShimmer component", () => {
  test("should render CollectionShimmer component", () => {
    render(<CollectionShimmer />);
  });
});

import { render } from "@testing-library/react";
import Shimmer from "..";

describe("TestCase for Shimmer component", () => {
  test("should render Shimmer component", () => {
    render(<Shimmer />);
  });
});

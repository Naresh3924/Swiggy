import { render } from "@testing-library/react";
import Collection from "../Collection";

describe("TestCase for Collection", () => {
  test("should render Collection component", () => {
    render(<Collection />);
  });
});
